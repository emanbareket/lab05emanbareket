The secret formula file is taking an array, passing each of its elements through an encryption function, and filling the
carray with the encrypted values. It then takes each element of the carray and passes them through a decryption function
which reverses the original encryption, and places those values which match the original array back into the marray.

i. yes, this holds for all entries in both arrays
ii. No, this only holds for the middle values of both arrays
